# Route ExecStart through wrapper: wait for hidraw node access before
# launching daemon (access via GROUP=lianli at node creation, no login needed).
[Service]
ExecStart=
ExecStart=%h/.local/lib/lianli-wrapper.sh

